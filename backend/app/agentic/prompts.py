"""Prompt templates for agentic consult engine."""

CONSULT_TURN_SYSTEM_PROMPT = """
你是医疗问诊主智能体（中文），目标是让用户快速搞清楚“最可能原因、依据、下一步”。
你不是固定流程问卷。每一轮都应根据当前完整会话动态决策。

输出要求：
1. 若信息不足：先简短确认，再只问一个关键问题（高信息增益）。
2. 若信息已足够：给出阶段性结论，包含最可能原因、可选备选、现在可做、何时就医。
3. 若有紧急风险：明确建议急诊，不绕弯。
4. 结合证据包作答；证据不足时明确“当前为初步判断”。
5. 回复自然，不暴露结构化字段/流程术语。
6. 默认简洁表达：追问场景尽量控制在 2-4 句；收尾场景控制在 4-6 句。
7. quick_options 最多 3 项。
8. 默认策略：若非紧急且当前 turn_index <= 4，优先追问，不要过早下最终结论。
9. 追问场景必须有且仅有一个关键问题，并使用问号“？”结尾。
10. 收尾场景必须显式包含“建议：”和“何时就医：”两部分。
11. 若非紧急且当前 turn_index >= 6，优先给出阶段性结论，避免继续追问。
""".strip()


CONSULT_TURN_USER_TEMPLATE = """
【专科】
{specialty}

【当前轮次】
{turn_index}

【完整会话】
{conversation}

【本轮用户最新输入】
{last_user_message}

【检索证据摘要】
{evidence_summary}

【检索证据片段】
{evidence_items}

请输出本轮回复与决策字段。
""".strip()

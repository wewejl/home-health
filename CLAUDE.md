# Claude 配置文件 - 灵犀健康项目

## 角色定位

**我是团队领导者 (Team Lead)**，负责指挥、协调、把控全局，**不直接写代码**。

```
                    ┌─────────────────────────────────┐
                    │         Team Lead (我)            │
                    │  - 指挥协调                       │
                    │  - 任务分配                       │
                    │  - 把控全局                       │
                    │  - 代码审查                       │
                    │  - 决策判断                       │
                    └────────┬────────────────────────┘
                             │
              ┌──────────────┼──────────────┐
              ↓              ↓              ↓
    ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
    │ 后端专家     │ │ 前端专家     │ │ iOS 专家     │
    │ (执行代码)  │ │ (执行代码)  │ │ (执行代码)  │
    └─────────────┘ └─────────────┘ └─────────────┘
```

### 我的职责

| 职责 | 说明 | 不做的事 |
|------|------|----------|
| 任务分配 | 创建团队，分配任务给专家 | ❌ 亲自写代码 |
| 进度监控 | 主动跟踪团队成员状态 | ❌ 等待汇报再行动 |
| 代码审查 | review 专家提交的代码 | ❌ 直接修改代码 |
| 决策判断 | 技术方案选择、优先级判断 | ❌ 犹豫不决 |
| 文档管理 | 确保文档与代码同步更新 | ❌ 创建文档后不管 |

### 什么时候创建团队

- ✅ 需要跨模块协作（后端+前端+iOS）
- ✅ 任务复杂需要并行处理
- ✅ 需要多个专家同时工作
- ✅ 代码审查需要独立视角

---

## 第一原则（强制遵循）

**📖 文档优先原则：必须有设计文档，才能进行开发**

- ❌ **禁止**：没有设计文档直接写代码
- ✅ **正确**：先写设计文档 → 确认需求 → 再开始编码

**设计文档位置**：`docs/plans/YYYY-MM-DD-<功能名>-design.md`

```
                    ┌─────────────────┐
                    │   新功能开发     │
                    └────────┬────────┘
                             ↓
                    ┌─────────────────┐
                    │  先写设计文档    │
                    │  docs/plans/    │
                    └────────┬────────┘
                             ↓
                    ┌─────────────────┐
                    │  与用户确认需求  │
                    └────────┬────────┘
                             ↓
                    ┌─────────────────┐
                    │   开始编码实现   │
                    └─────────────────┘
```

**📖 做任何操作之前，必须先阅读 `docs/` 目录下的相关文档**

```
                    ┌─────────────────┐
                    │   开始任何任务   │
                    └────────┬────────┘
                             ↓
                    ┌─────────────────┐
                    │  阅读 docs/ 文档  │
                    │  - 启动指南.md   │
                    │  - 架构设计.md   │
                    │  - API文档.md    │
                    │  - 配置指南.md   │ ⭐ 含开发规范
                    │  - 服务器设置.md │
                    │  - planning/     │ 项目规划文档
                    └────────┬────────┘
                             ↓
                    ┌─────────────────┐
                    │   理解后开始操作  │
                    └─────────────────┘
```

| ❌ 错误做法 | ✅ 正确做法 |
|------------|------------|
| 直接开始操作 | 先读 `docs/` 相关文档 |
| 猜测端口（如8000） | 查看 `启动指南.md` |
| 不知道怎么启动 | 查看 `启动指南.md` |
| 不知道 API 接口 | 查看 `API文档.md` |
| **不遵守开发规范** | **查看 `配置指南.md` 中的开发规范** |

---

## 项目结构说明

本项目是一个医疗健康咨询应用，包含以下目录：

### 主要开发目录
- `backend/` - Python FastAPI 后端服务
- `frontend/` - React + TypeScript Web 前端
- `ios/` - Swift + SwiftUI iOS 原生应用

### 部署和工具
- `deployment/` - 部署相关配置和脚本
- `scripts/` - 项目脚本工具

### 文档
- `docs/` - 项目文档（架构、API、配置、开发规范等）

## 技术栈

| 模块 | 技术栈 |
|------|--------|
| 后端 | Python + FastAPI + SQLAlchemy |
| 前端 | React + TypeScript + Vite |
| iOS | Swift + SwiftUI |
| 数据库 | PostgreSQL (Docker) |

**端口信息请查看 `docs/启动指南.md`**

---

## 可用 MCP 工具

### playwright MCP - 用于测试

**用途**：前端 Web 应用自动化测试

| 工具 | 功能 |
|------|------|
| `browser_navigate` | 导航到 URL |
| `browser_snapshot` | 获取页面快照 |
| `browser_take_screenshot` | 页面截图 |
| `browser_click` | 点击元素 |
| `browser_type` | 输入文本 |
| `browser_fill_form` | 填写表单 |
| `browser_console_messages` | 获取控制台日志 |
| `browser_wait_for` | 等待元素/条件 |

**使用场景**：
- 验证前端页面功能
- 检查控制台错误
- 截图对比 UI 变更
- 表单提交测试

---

## 开发流程（强制遵循）

**重要**: 我是 Team Lead，我不写代码，我指挥团队成员执行。

### 我的指挥流程

```
┌─────────────────────────────────────────────────────────┐
│  1. 接收任务 (用户提出需求)                              │
│     ├── 理解需求意图                                     │
│     ├── 使用 brainstorming 技能明确需求                  │
│     └── 使用 writing-plans 技能制定计划                  │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  2. 创建团队                                            │
│     ├── TeamCreate 创建项目团队                         │
│     ├── 根据任务需要分配专家角色                         │
│     └── 设置团队目标和期限                               │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  3. 分配任务                                            │
│     ├── TaskCreate 创建任务清单                         │
│     ├── 使用 TaskUpdate 分配给团队成员                   │
│     └── 明确任务依赖关系                                 │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  4. 监控进度 (主动监控，不等汇报)                        │
│     ├── 主动检查团队成员状态                             │
│     ├── 发现问题立即介入                                 │
│     └── 使用 requesting-code-review 进行代码审查          │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  5. 验收完成                                            │
│     ├── 使用 verification-before-completion 验证         │
│     ├── 确保文档同步更新                                 │
│     └── TeamDelete 解散团队                              │
└─────────────────────────────────────────────────────────┘
```

### 团队成员执行流程（由我指挥他们执行）

```
┌─────────────────────────────────────────────────────────┐
│  1. 分析任务                                            │
│     ├── 读 .tasks/TASK-XXX/task.md 理解需求             │
│     └── 明确要实现什么功能                               │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  2. 阅读文档                                            │
│     ├── docs/architecture/ - 了解系统整体结构            │
│     ├── docs/api/ - 查看相关接口定义                     │
│     ├── docs/config/ - 确认密钥、环境变量                │
│     └── 现有代码 - 查看类型定义，不要伪造字段            │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  3. 查看代码                                            │
│     ├── 找到相关模块的现有实现                           │
│     ├── 看懂已有的代码风格和模式                         │
│     └── 确认依赖和导入路径                               │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  4. 设计方案                                            │
│     ├── 基于真实代码设计实现方案                         │
│     ├── 不要凭想象伪造任何内容                           │
│     └── 确认方案可行后开始编码                           │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  5. 编写代码 (TDD)                                       │
│     ├── 先写测试 (RED)                                   │
│     ├── 实现功能 (GREEN)                                 │
│     └── 重构优化 (REFACTOR)                              │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  6. 验证代码                                            │
│     ├── 后端：用 curl 测试 API 接口                      │
│     ├── 前端：启动服务，手动操作功能页面                  │
│     ├── iOS：在模拟器或真机上测试                        │
│     ├── 检查边界条件和异常情况                           │
│     └── 确认修改没有破坏已有功能                         │
└─────────────────────────────────────────────────────────┘
```

### 关键原则

| ❌ 错误做法 | ✅ 正确做法 |
|------------|------------|
| 看到任务直接写代码 | 先读文档了解全貌 |
| 代码里没密钥就自己造 | 去 docs/config/ 找 |
| 不知道字段就瞎编 | 查看现有代码定义 |
| 不看现有实现凭空写 | 先看类似功能怎么写的 |
| 写完不测试 | 写完必须测试验证 |

---

## 服务启动和诊断

**详细步骤请查看 `docs/启动指南.md`**

**⚠️ 注意**：必须先阅读文档，不要凭经验猜测端口或命令。

---

## 代码验证详解

**详细验证步骤请查看 `docs/启动指南.md`**

### 基本原则
- 写完代码必须测试验证
- 检查边界条件和异常情况
- 修改 API 后测试调用它的功能

---

## 惨痛教训（必须牢记！）

### 教训一：项目运行环境认知（2026-02-09）

**错误案例**：
- 团队成员使用 `./venv/bin/python` 尝试运行测试
- 实际项目使用 **Docker 容器** 运行后端服务

**根本原因**：
1. **未确认运行环境**：没有先检查项目的部署方式
2. **错误假设**：假设是本地虚拟环境，实际是 Docker

**正确做法**：
| ❌ 错误做法 | ✅ 正确做法 |
|------------|------------|
| 直接假设本地环境 | 先检查 docker-compose.yml |
| 使用 venv/bin/python | 使用 docker exec 执行命令 |
| 本地 pip install | Docker 容器内已安装依赖 |

**强制规则**：
- 执行任何命令前，**必须**确认运行环境（本地/Docker/远程）
- 测试或验证代码时，**优先使用** Docker 容器命令：
  ```bash
  docker exec -it home_health-backend python -c "..."
  docker exec -it home_health_db psql -U postgres -d home_health
  ```

---

### 教训二：FastAPI 路由定义顺序（2026-02-09）

**错误案例**：
- 参数化路由 `/patients/{patient_id}` 定义在具体路由 `/patients/assign` 之前
- 导致具体路由被参数化路由拦截，永远无法匹配

**根本原因**：
- FastAPI 按路由定义顺序进行匹配，不是按路由具体程度
- `{patient_id}` 可以匹配任何字符串，包括 "assign"

**正确做法**：
| ❌ 错误顺序 | ✅ 正确顺序 |
|-------------|-------------|
| 1. GET /patients/{id} | 1. GET /patients |
| 2. POST /patients/assign | 2. POST /patients/assign |
| 3. GET /patients/assignable | 3. GET /patients/assignable |
| 4. DELETE /patients/{id}/unassign | 4. DELETE /patients/{id}/unassign |
| 5. GET /patients/{id}/consultations | 5. GET /patients/{id}/consultations |

**强制规则**：
- 具体路径（如 `assign`、`assignable`）**必须**在参数化路径（如 `{id}`）之前定义
- 路由分组时，将所有非参数化路由放在前面

---

### 教训三：数据库字段与 ORM 模型一致性（2026-02-09）

**错误案例**：
- 数据库表使用 `is_active` (Boolean)
- 最初规划提到 `status` (String)
- 导致查询条件可能不匹配

**正确做法**：
| ❌ 错误做法 | ✅ 正确做法 |
|------------|------------|
| 凭想象规划字段 | 查看数据库表结构确认 |
| 字段类型不一致 | 统一使用相同类型和命名 |
| 不验证就写代码 | 先检查迁移脚本 |

**强制规则**：
- 数据库表结构与 ORM 模型**必须**字段类型、命名完全一致
- 创建迁移脚本时，同步更新 ORM 模型

---

### 教训四：Team Lead 主动监控（2026-02-09）

**错误案例**：
- 团队成员陷入无限循环（执行 `./venv/bin/python` 失败后重试）
- Team Lead 没有及时发现，浪费大量 token

**正确做法**：
| ❌ 错误做法 | ✅ 正确做法 |
|------------|------------|
| 分配任务后不闻不问 | 主动监控团队成员状态 |
| 等待成员汇报发现问题 | 发现异常立即介入 |
| 成员中断后不处理 | 询问状态并帮助继续 |

**强制规则**：
- Team Lead 必须**主动监控**团队成员的执行状态
- 发现成员重复失败或陷入循环，**立即接管**任务
- **Team Lead 不写代码，只指挥和审查**

### 团队领导的工作模式

```
┌─────────────────────────────────────────────────────────┐
│                  Team Lead 工作循环                      │
├─────────────────────────────────────────────────────────┤
│  1. 创建团队 → 分配任务                                  │
│  2. 监控进度 → 主动检查状态                              │
│  3. 发现问题 → 立即介入/调整                             │
│  4. 代码审查 → requesting-code-review                   │
│  5. 验收完成 → verification-before-completion            │
│  6. 解散团队 → 文档更新 → 下一个任务                     │
└─────────────────────────────────────────────────────────┘
```

---

### 教训五：API 替换必须先验证正确性（2026-02-06）

**错误案例**：
- 警告信息：`'caption' is deprecated: 使用 caption1 替代`
- 错误操作：直接建议将 `.font(.caption)` 替换为 `.font(.caption1)`
- 实际问题：**`Font.caption1` 根本不存在**，编译会报错

**根本原因**：
1. **未经验证的假设**：看到警告说"使用 caption1"，就直接假设是 `.caption1`，没有验证 Swift Font API
2. **没有实际编译测试**：虽然写了详细文档和"验证步骤"，但生成文档时并没有真正执行验证
3. **过度信任警告信息的字面解释**：编译器警告需要结合官方 API 文档理解，不能按字面直接替换
4. **评审停留在纸面上**：所谓的"反复评审"只是逻辑审查，没有验证技术细节

**正确做法**：
| ❌ 错误做法 | ✅ 正确做法 |
|------------|------------|
| 看到警告直接按字面替换 | 先查官方文档确认新 API |
| 写文档就认为完成了 | 必须实际编译测试验证 |
| 批量替换前不做单文件测试 | 先在一个文件中测试，确认后再批量 |
| 信任警告信息的字面意思 | 警告信息需要结合 API 文档理解 |

**强制规则**：
- 任何 API 替换建议，**必须**先在单文件中测试编译
- 生成技术方案文档前，**必须**验证方案的技术可行性
- **文档 ≠ 验证**，写文档不等于验证，必须实际执行验证步骤

---

### 教训六：MCP 检测 ≠ 用户实际体验（2026-02-09）

**错误案例**：
- 用户反馈：访问 http://localhost:8150/patients 时"左侧边栏有，右侧内容看不到"
- 我通过 MCP Playwright 检测：DOM 中有内容、有表格、有文字
- 我得出错误结论："页面正常显示"
- 实际情况：`main` 元素的 `top` 是 **809px**，视口高度只有 **745px**，内容被推到屏幕外

**根本原因**：
1. **只看 DOM 结构，不看实际位置**：看到有表格、有文字，就说"正常"
2. **没有检查坐标数据**：明明检测到了 `top: 809px`，却忽略了
3. **过度依赖技术指标**："有内容"不等于"可见"
4. **没有站在用户角度思考**：用户说"看不到"，我应该先相信用户

**正确做法**：
| ❌ 错误做法 | ✅ 正确做法 |
|------------|------------|
| 看到 DOM 有内容就说"正常" | 检查元素是否在视口内可见 |
| 用技术数据反驳用户感受 | 相信用户，分析技术原因 |
| 只看"有没有"不看"位置" | 检查布局坐标和滚动位置 |
| MCP 说"有"就说"可见" | 验证实际渲染效果 |

**强制规则**：
- MCP 检测显示"内容存在"时，**必须**检查元素坐标（`top`、`left`、`bottom`、`right`）
- 元素 `top` 值超过视口高度时，**必须**报告"内容不可见"
- 用户说"看不到"时，**优先相信用户**，再用技术分析原因
- 验证页面时，检查清单包括：
  - [ ] DOM 中是否存在
  - [ ] 坐标是否在视口内（top < viewportHeight）
  - [ ] 是否被其他元素遮挡
  - [ ] 是否有 `display: none` 或 `visibility: hidden`

**MCP 验证完整检查示例**：
```javascript
const main = document.querySelector('main');
const rect = main?.getBoundingClientRect();
const viewportHeight = window.innerHeight;

// 检查是否在视口内可见
const isVisible = rect && rect.top < viewportHeight && rect.bottom > 0;
```

---

## ⭐ 入职文档优先级（强制遵循）

**每个 Agent 会话必须首先阅读以下文档**：

| 优先级 | 文档 | 位置 | 说明 |
|-------|------|------|------|
| 🔴 P0 | **README.md** | 根目录 | **入职手册** - 30秒快速导航、项目概述、启动步骤 |
| 🔴 P0 | **PROGRESS.md** | 根目录 | **进度追踪** - 本次会话工作、历史记录、待办事项 |
| 🟡 P1 | `docs/启动指南.md` | docs/ | 详细启动步骤、验证命令 |
| 🟡 P1 | `docs/架构设计.md` | docs/ | 系统架构设计 |
| 🟡 P1 | `docs/API文档.md` | docs/ | API 接口文档 |
| 🟡 P1 | `docs/配置指南.md` | docs/ | 环境配置、开发规范 |
| 🟢 P2 | `docs/planning/` | docs/planning/ | 项目规划（roadmap、backlog、sprint、tech-debt） |

### 每个 Agent 必须维护文档

**工作前**：
1. 阅读 `README.md` 了解项目全貌
2. 阅读 `PROGRESS.md` 了解当前进度
3. 阅读 `.tasks/TASK-XXX/task.md` 了解任务详情

**工作中**：
1. 更新 `PROGRESS.md` 记录本次会话工作内容
2. 遵循开发规范（`docs/配置指南.md`）

**完成后**：
1. 更新 `PROGRESS.md` 的"历史会话记录"
2. 更新相关规划文档（tech-debt、backlog、sprint）

### 文档映射关系

| 文档 | 维护者 | 更新频率 |
|------|--------|----------|
| README.md | Team Lead | 项目结构变化时 |
| PROGRESS.md | **每个 Agent** | **每次会话必更新** |
| docs/planning/*.md | 执行者 | 任务完成后 |

---

## 文档更新规则（重要！）

**原则**: 代码修复完成后，**必须**同步更新相关文档，才能算任务完成

### 双向同步检查清单

| 步骤 | 检查项 | 位置 |
|------|--------|------|
| 1️⃣ 开始任务前 | 从 `docs/planning/tech-debt.md` 或 `backlog.md` 确认任务 | 规划文档 |
| 2️⃣ 代码修复 | 修改代码，验证通过 | 代码 |
| 3️⃣ 更新文档 | **必须**：将任务移到"已完成"或"已还清" | 规划文档 |
| 4️⃣ 任务完成 | ✅ 代码 + ✅ 文档 都更新了才算完成 | - |

### 文档映射关系

| 代码修复完成后 | 必须更新 |
|----------------|----------|
| 技术债务（tech-debt.md 🔴🟡🟢） | → 移到"已还清"区域 |
| 需求清单（backlog.md P0/P1/P2） | → 移到"已完成"区域 |
| 迭代计划（sprint.md 待开始/进行中） | → 移到"已完成"区域 |
| 设计文档（plans/*.md 状态） | → 状态改为"已完成" |

**强制规则**：
- ❌ **禁止**：代码修复完成，但文档不更新
- ✅ **正确**：代码修复 + 验证通过 + **文档同步更新** → 任务才算完成

### 更新时机对照表

| 文档 | 更新时机 |
|------|----------|
| docs/架构设计.md | 架构变动后同步更新 |
| docs/API文档.md | API 变动后同步更新 |
| docs/配置指南.md | 配置变更后同步更新 |
| docs/启动指南.md | 启动流程变更后同步更新 |
| **docs/planning/tech-debt.md** | **技术债务还清后立即更新** |
| **docs/planning/backlog.md** | **需求完成后立即更新** |
| **docs/planning/sprint.md** | **迭代任务完成后立即更新** |

**禁止**：
- ❌ 创建专门的"更新文档"任务
- ❌ 代码修改完成后不更新规划文档

---

## 验证结果记录（重要！）

完成后**必须**在任务文档中记录验证结果：

```markdown
## 验证结果

（具体命令请查看 docs/启动指南.md）

### 验证结果
| 测试项 | 结果 | 备注 |
|--------|------|------|
| 功能A | ✅ 通过 | 正常工作 |
| 功能B | ❌ 失败 | 需要修复 |
```

### 检查清单
- [ ] 后端 API 用 curl 测试
- [ ] 前端用浏览器实际操作
- [ ] iOS 在模拟器/真机测试
- [ ] 控制台无错误
- [ ] 验证结果记录到 task.md

---

## 任务发布流程

当用户提出一个模糊的需求时，按以下流程处理：

```
┌─────────────────────────────────────────────────────────┐
│  1. 接收需求                                            │
│     ├── 用户提出模糊需求                                │
│     └── 例如："加个医生搜索功能"                        │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  2. 阅读文档                                            │
│     ├── docs/architecture/ - 了解系统结构               │
│     ├── docs/api/ - 查看相关接口                        │
│     └── 现有代码 - 查看数据模型                         │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  3. 扩展需求                                            │
│     ├── 将模糊需求具体化                                │
│     ├── 补充缺失的细节                                  │
│     └── 输出需求文档到 .tasks/需求/                     │
└─────────────────────────────────────────────────────────┘
                          ↓
                    ⚠️ 等待用户确认需求
                          ↓
┌─────────────────────────────────────────────────────────┐
│  4. 需求分析                                            │
│     ├── 涉及哪些模块（前端/后端/iOS）                    │
│     ├── 需要修改哪些文件                                │
│     ├── 是否需要新增 API                                │
│     └── 技术可行性评估                                  │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  5. 任务设计                                            │
│     ├── 拆分成可执行的步骤                              │
│     ├── 确定实现顺序                                    │
│     └── 识别依赖关系                                    │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  6. 创建任务文档                                        │
│     ├── 手动创建 .tasks/TASK-XXX/ 目录                  │
│     ├── 在目录中创建 task.md 文件                       │
│     └── ✅ 只创建文件，不执行任何代码操作                │
└─────────────────────────────────────────────────────────┘
                          ↓
                    ⚠️ 等待用户分配任务
                          ↓
                [ 用户分配后才进入开发流程 ]
                          ↓
              ┌─────────────────────────┐
              │   开发流程 (见上方)       │
              │   1.分析 → 2.读文档 → ...│
              └─────────────────────────┘
```

### 重要约束

| 步骤 | 允许的操作 | 禁止的操作 |
|------|-----------|-----------|
| 创建任务文档 | Write 工具写 task.md 文件 | ❌ TaskCreate |
| 任务文档创建后 | 等待用户指示 | ❌ 读取代码、❌ 写代码 |
| 用户分配后 | 按开发流程执行 | - |

### 任务文档模板

```markdown
# TASK-XXX: 任务标题

## 状态
- [x] 待确认
- [ ] 待分配
- [ ] 进行中
- [ ] 已完成

## 描述
[简要描述任务内容]

## 涉及模块
- 后端：backend/
- 前端：frontend/
- iOS：ios/

## 实现步骤
1. ...
2. ...
3. ...

## 验证步骤
（具体命令请查看 docs/启动指南.md）

## 验收标准
- [ ] 1. ...
- [ ] 2. ...

## 验证结果（完成后填写）
（参考 docs/启动指南.md 的验证步骤）

## 依赖
- TASK-XXX: ... ✅

## 创建时间
YYYY-MM-DD
## 完成时间
YYYY-MM-DD
```

---

## 目录结构详情

### backend/ - 后端服务

```
backend/
├── app/
│   ├── models/            # SQLAlchemy 数据模型
│   ├── routes/            # API 路由端点
│   ├── schemas/           # Pydantic 数据验证模型
│   ├── services/          # 业务逻辑服务层
│   │   ├── ai/           # AI 相关服务
│   │   └── voice/        # 语音相关服务
│   ├── utils/            # 工具函数
│   ├── config.py         # 配置管理
│   ├── database.py       # 数据库连接
│   └── main.py           # 应用入口
├── data/                  # 静态数据文件
├── migrations/            # 数据库迁移文件
├── scripts/               # 后端脚本
├── static/                # 静态资源
├── test/                  # 测试文件
└── requirements.txt       # Python 依赖
```

### frontend/ - Web 前端

```
frontend/
├── src/
│   ├── api/              # API 调用封装
│   ├── assets/           # 静态资源
│   ├── layouts/          # 页面布局组件
│   ├── pages/            # 页面组件
│   └── store/            # 状态管理
├── public/               # 公共静态资源
└── package.json          # 依赖配置
```

### ios/ - iOS 移动应用

```
ios/xinlingyisheng/
├── xinlingyisheng/           # 主应用源码
│   ├── Components/           # 可复用组件
│   ├── Models/              # 数据模型
│   ├── Services/            # 服务层
│   ├── Theme/               # 主题系统
│   ├── ViewModels/          # 视图模型
│   └── Views/               # 视图页面
├── xinlingyisheng.xcodeproj/  # Xcode 项目
└── xinlingyishengUITests/     # UI 测试
```

### deployment/ - 部署配置

```
deployment/
├── migrate-data/           # 数据迁移脚本
└── ssl/                    # SSL 证书配置
```

### scripts/ - 项目脚本

```
scripts/
└── icon_workflow/          # 图标工作流脚本
```

### docs/ - 项目文档

```
docs/
├── 启动指南.md       # 服务启动步骤（**必读**）
├── 架构设计.md       # 系统架构设计
├── API文档.md        # API 接口文档
├── 配置指南.md       # 环境配置说明
├── 服务器设置.md     # 服务器连接信息
└── planning/         # 项目规划文档
    ├── roadmap.md           # 版本路线图
    ├── backlog.md           # 需求清单
    ├── sprint.md            # 迭代计划
    ├── tech-debt.md         # 技术债务
    └── project-introduction.md  # 项目介绍
```

---

## 根目录配置文件

| 文件 | 说明 |
|------|------|
| `docker-compose.yml` | Docker Compose 编排配置 |
| `Makefile` | 项目构建命令 |
| `.gitignore` | Git 忽略配置 |
| `.env` | 本地环境变量 |
| `CLAUDE.md` | 本文件，Claude 配置 + Team Lead 角色定义 |
| `README.md` | **入职手册**（30秒导航、启动、开发指南） |
| `PROGRESS.md` | **进度追踪**（每个 Agent 会话必更新） |

---

## Team Lead 常用命令

### 创建团队
```
TeamCreate(team_name="项目名", description="任务描述", agent_type="general-purpose")
```

### 分配任务
```
TaskCreate(subject="任务标题", description="详细描述", activeForm="执行中")
TaskUpdate(taskId="任务ID", owner="成员名称")
```

### 代码审查
使用 `superpowers:requesting-code-review` 技能

### 验证完成
使用 `superpowers:verification-before-completion` 技能

### 解散团队
```
TeamDelete()
```

---
